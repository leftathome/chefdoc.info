<div id="content" class="scm fof">
  <h1>Oops,</h1>
  <% if settings.opscode_adapter.libraries.has_key?(@gemname) %>
    <h2 class="nomargin">We Couldn't Find That Page</h2>
    <p>Sorry, but we couldn't find the page <strong><%= request.path %></strong> in the <strong><%= @gemname %></strong> cookbook. Are you sure that Chef object exists?</p>
  <% else %>
    <h2 class="nomargin">We Couldn't Find That Cookbook</h2>
    <p>We can't seem to find any versions for the cookbook <strong><%= @gemname %></strong>. Are you sure you typed it correctly?</p>
  <% end %>

  <p>You can also see a list of cookbooks known to the Opscode community site API <a href="/opscode">here</a>.</p>
</div>
